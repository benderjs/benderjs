<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= name %></title>
    <% if (css) { %><link rel="stylesheet" href="<%= css %>"><% } %>
</head>
<body>
    <%= html %>

    <% scripts && scripts.forEach(function (script) { %>
        <script src="<%= script %>"></script>
    <% }) %>

    <script>
        window.parent.bender.setup(window);
        window.bender.ready();
    </script>
</body>
</html>
